<h1><%= @wiki.title %></h1>

<div class="row">
	<div class="col-md-8">
		<p> <%= @wiki.body %></p>
	</div>

	<% if @wiki.user = current_user %>
		<div class="col-md-8">
			<%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-success' %>
		</div>
	<% end %>
</div>
